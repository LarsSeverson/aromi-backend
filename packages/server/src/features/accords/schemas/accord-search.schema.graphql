type SearchAccordEdge {
  node: Accord!
  offset: Int!
}

type SearchAccordConnection {
  edges: [SearchAccordEdge!]!
  pageInfo: SearchPageInfo!
}

extend type Query {
  searchAccords(input: SearchInput): SearchAccordConnection!
}
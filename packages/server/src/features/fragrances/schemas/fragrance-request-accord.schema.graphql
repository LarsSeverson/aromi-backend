input SetFragranceRequestAccordsInput {
  requestId: ID!
  accordIds: [ID!]!
}

extend type Mutation {
  setFragranceRequestAccords(
    input: SetFragranceRequestAccordsInput!
  ): FragranceRequest!
}

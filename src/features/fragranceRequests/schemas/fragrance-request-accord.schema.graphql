input SetFragranceRequestAccordsInput {
  requestId: ID!
  version: Int!
  accordIds: [ID!]!
}

extend type Mutation {
  setFragranceRequestAccords(
    input: SetFragranceRequestAccordsInput!
  ): FragranceRequest!
}

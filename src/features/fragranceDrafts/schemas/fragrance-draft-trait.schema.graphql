type FragranceDraftTrait {
  traitType: TraitTypeEnum!
  selectedOption: TraitOption
}

input SetFragranceDraftTraitInput {
  draftId: ID!
  version: Int!
  score: Int!
  traitType: TraitTypeEnum!
}

extend type Mutation {
  setFragranceDraftTrait(input: SetFragranceDraftTraitInput!): FragranceDraft!
}

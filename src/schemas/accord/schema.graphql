type Accord {
  id: Int!
  name: String!
  color: String!
  audit: Audit!
}

extend type Query {
  accords(input: PaginationInput): AccordConnection!
}

type AccordEdge {
  cursor: String!
  node: Accord!
}

type AccordConnection {
  edges: [AccordEdge!]!
  pageInfo: PageInfo!
}
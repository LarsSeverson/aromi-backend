type PageInfo {
  hasNextPage: Boolean!
  hasPreviousPage: Boolean!
  startCursor: String
  endCursor: String
}

# input PaginationInput {
#   first: Int
#   after: String
#   sort: SortByInput
# }

enum SortDirection {
  ASCENDING
  DESCENDING
}
